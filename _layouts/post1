---
layout: default
---
<article class="post h-entry" itemscope itemtype="http://schema.org/BlogPosting">
    <div class="wrapper post-wrapper">
        
        <header class="post-header">
            <h1 class="post-title p-name" itemprop="name headline">{{ page.title | escape }}</h1>
            <p class="post-meta">
                <time class="dt-published" datetime="{{ page.date | date_to_xmlschema }}" itemprop="datePublished">
                    {{ page.date | date: "%d/%m/%Y" }}
                </time>
                {%- if page.author -%}
                    <span class="meta-separator">•</span>
                    <span itemprop="author" itemscope itemtype="http://schema.org/Person">
                        <span class="p-author h-card" itemprop="name">Di {{ page.author }}</span>
                    </span>
                {%- endif -%}
            </p>
        </header>

        <div class="post-content e-content" itemprop="articleBody">
            {{ content }}
        </div>

        <hr class="section-divider">

        <div class="author-box">
            <div class="author-avatar-placeholder">✍️</div>
            <div class="author-info">
                <h3 class="author-name">{{ page.author | default: site.author }}</h3>
                <p class="author-bio">Appassionato di lingue e poliglotta. Su <strong>{{ site.title }}</strong> condivido metodi e risorse per aiutarti a studiare in autonomia, ottimizzando tempo e motivazione.</p>
            </div>
        </div>

    </div>
</article>
